<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/assets/common-style.css">
    <link rel="stylesheet" href="/assets/components-style.css">
    <link rel="stylesheet" href="/assets/highlight.css">
  </head>
  <body>
    <div style="width: 100%; height: 100%; overflow: auto;">
      <%- include('header') %>
      <div class="flex" style="min-height: calc(100vh - 120px);">
        <div class="flex justify-center items-start" style="width: 25%;">
          <% if (toc && !isMobile) { %>
            <%- toc %>
          <% } %>
        </div>
        <div class="flex-g-1">
          <div class="flex justify-start"><h1 class="post-title"><%= pageTitle %></h1></div>
          <div class="flex">
            <% for( var i = 0; i < tags.length; i++ ) { %>
              <div class="tag-item"><a href="/tags/<%= tags[i] %>" style="color: rgb(102, 102,102);">#<%= tags[i] %></a></div>
            <% } %>
          </div>
          <p style="font-family: sans-serif; font-size: 14px; color: rgb(102, 102,102);">Published <%= date %></p>
          <%- content %>
        </div>
        <div style="width: 25%"></div>
      </div>
      <%- include('footer') %>
    </body>
    </div>
</html>
